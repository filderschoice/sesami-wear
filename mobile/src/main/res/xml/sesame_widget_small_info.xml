<?xml version="1.0" encoding="utf-8"?>
<!--
  ホーム画面ウィジェットの「2 × 1」版（BL-186）。表示・操作は sesame_widget_info.xml と同じで、
  違うのは初期サイズだけ（targetCellWidth/Height と、API 30 以下で効く minWidth/minHeight）。
  Android は1つの provider へ初期サイズを1つしか持たせられないため、
  「置いた直後から小さく使いたい」を満たすには provider をもう1つ宣言する必要がある。
  縮小の下限（minResizeWidth/minResizeHeight）と再設定の可否は 4x2 版と揃える。
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:minWidth="110dp"
    android:minHeight="50dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="50dp"
    android:targetCellWidth="2"
    android:targetCellHeight="1"
    android:resizeMode="horizontal|vertical"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/glance_default_loading_layout"
    android:configure="com.sesamiwear.mobile.widget.WidgetConfigurationActivity"
    android:widgetFeatures="reconfigurable"
    android:description="@string/widget_description_small"
    android:widgetCategory="home_screen"
    tools:ignore="UnusedAttribute" />
