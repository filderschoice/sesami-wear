<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution">

    <dist:module
        dist:instant="false"
        dist:title="@string/wear_module_title">
        <dist:delivery>
            <dist:install-time>
                <dist:conditions>
                    <dist:device-feature dist:name="android.hardware.type.watch" />
                </dist:conditions>
            </dist:install-time>
        </dist:delivery>
        <dist:fusing dist:include="true" />
    </dist:module>

    <uses-feature android:name="android.hardware.type.watch" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name">

        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="false" />

        <!--
            ランチャーアイコンは持たない（BL-066）。以前はLAUNCHER intent-filterを持っていたが、
            baseモジュール（mobile）は常にウォッチ側にもインストールされるため、
            mobile.MainActivityと合わせてウォッチのアプリ一覧にアイコンが2つ表示される問題が
            あった。ウォッチ上での主要な操作導線はTile/Complicationであり本Activityの独立起動は
            不要なため、LAUNCHER属性を除去して1アイコンに統一する。mobile.MainActivityが
            ウォッチ実行時にexplicit Intent（クラス名文字列）で本Activityへ委譲するため、
            exported属性は不要（同一アプリ内からの起動はexported=falseでも可能）。
            noHistory/excludeFromRecentsは以下の5つのActivity共通の対応（BL-067）: いずれも
            taskAffinityを明示指定しておらずデフォルト（アプリ共通）のため、Tileタップによる
            LaunchAction起動とmobile.MainActivityからの委譲起動が同一タスクへ積み重なり、
            finish()後に無関係な古いActivity（本MainActivity）が背後から現れる不具合が
            ユーザー報告で判明した。noHistory="true"によりフォアグラウンドを外れた時点で
            即座に破棄されるようにし、タスクに残留して後続の起動に巻き込まれることを防ぐ。
        -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:noHistory="true"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.DeviceDefault" />

        <activity
            android:name=".action.SesameActionActivity"
            android:exported="true"
            android:noHistory="true"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.DeviceDefault" />

        <activity
            android:name=".action.SesameStatusRefreshActivity"
            android:exported="true"
            android:noHistory="true"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.DeviceDefault" />

        <activity
            android:name=".tile.TileConfigurationActivity"
            android:exported="true"
            android:noHistory="true"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.DeviceDefault" />

        <!--
            Complicationのデバイス割り当て画面。tapActionからの起動に加えて、文字盤のComplication
            ピッカーでデータソースを選んだ直後にシステムから起動される標準の設定導線
            （ComplicationDataSourceService.METADATA_KEY_DATA_SOURCE_CONFIG_ACTION）へ対応する
            ため、intent-filterを追加した（BL-073）。この導線が無いと、未設定Complicationの
            「タップして設定」を押す1経路でしかデバイスを割り当てられず、枠ごとに別デバイスを
            設定したり、設定済みの枠のデバイスを変更したりできなかった。
            actionは他アプリの設定画面と混同しないようアプリ固有の名前を用いる（システムは
            EXTRA_CONFIG_DATA_SOURCE_COMPONENTでデータソースを明示して起動する）。
        -->
        <activity
            android:name=".complication.ComplicationConfigurationActivity"
            android:exported="true"
            android:noHistory="true"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.DeviceDefault">
            <intent-filter>
                <action android:name="com.sesamiwear.wear.complication.CONFIG_COMPLICATION" />
                <category android:name="android.support.wearable.complications.category.PROVIDER_CONFIG" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <service
            android:name=".complication.SesameComplicationDataSourceService"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_wear"
            android:label="@string/app_name"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER">
            <intent-filter>
                <action android:name="android.support.wearable.complications.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>
            <!--
                SHORT_TEXTのみを宣言していると、LONG_TEXT枠しか受け付けない文字盤で本データソースを
                選べない（または選べても型不一致でデータが破棄され空欄になる）ため、LONG_TEXTも
                対応型に加えた（BL-072）。型ごとの表示内容はSesameComplicationDataSourceServiceが
                request.complicationTypeを見て作り分ける。
                UPDATE_PERIOD_SECONDSは、従来の0（定期更新なし・更新要求のみが契機）では
                ComplicationDataSourceUpdateRequesterからの更新要求が届かなかった場合に空欄のまま
                復帰しないため、定期更新ありへ変更した（Wear OSの実効最小間隔は300秒のため、
                消費電力を考慮して600秒とする）。
            -->
            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="SHORT_TEXT,LONG_TEXT" />
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="600" />
            <!--
                データソース選択直後にComplicationConfigurationActivityを起動させるための設定
                （BL-073）。値は上記Activityのintent-filterのactionと一致させる必要がある。
            -->
            <meta-data
                android:name="android.support.wearable.complications.PROVIDER_CONFIG_ACTION"
                android:value="com.sesamiwear.wear.complication.CONFIG_COMPLICATION" />
        </service>

        <service
            android:name=".tile.SesameTileService"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_wear"
            android:label="@string/app_name"
            android:permission="com.google.android.wearable.permission.BIND_TILE_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.tiles.action.BIND_TILE_PROVIDER" />
            </intent-filter>
        </service>

        <service
            android:name=".messaging.SesameResultListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:pathPrefix="/sesami-wear/result" />
            </intent-filter>
        </service>

        <service
            android:name=".messaging.SesameStatusListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:pathPrefix="/sesami-wear/status" />
            </intent-filter>
        </service>
    </application>
</manifest>
