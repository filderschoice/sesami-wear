<!-- markdownlint-disable-file MD041 -->
<!-- Copilot専用未対応事項記録ファイル（ユーザ編集禁止） -->
<!-- このファイルはCopilotがプロンプト指示の処理実行時のみ自動更新します。 -->
<!-- schema: records.backlog.v1 -->

<!-- COPILOT_RECORDS:BEGIN -->
```yaml
- id: BL-191
  区分: 性能
  タスク内容: >-
    BLEの失敗が1回あっただけで、次の到達確認まで（最短5分）BLEを試さなくなる挙動を見直す。
    `mobile.command.SesameBleAccess.recordAttempt`は失敗すると`SesameBleReachability.record`へ
    `reachable=false`を渡し、到達実績を消す。到達実績が無い間は`allowsBle`が偽になるため、
    以降の操作はBLEを試さずインターネット経由になり、`shouldProbe`が許す次の到達確認
    （`PROBE_INTERVAL_MILLIS`＝5分）まで復帰しない。
    2026-09-20の実機検証では、BLEの失敗理由が`NOT_FOUND`（圏外）だけでなく`CONNECTION_FAILED`・
    `LOGIN_FAILED`（見つかっているが時間内に繋げない・ログインできない）でも起きた。
    後者は圏内にいる証拠なので、到達実績を消さずに次の操作で再試行したほうが
    「ある程度諦めない」方針（2026-09-20のユーザー指示）に沿う。
    実装は、`SesameBleOperations`の実行結果へ失敗理由（またはデバイスを見つけられたか）を載せ、
    `recordAttempt`が`NOT_FOUND`のときだけ到達実績を消す形にする。
    連続で失敗し続ける場合に毎回BLEの上限（3秒）を払い続けないよう、連続失敗回数で打ち切る仕組みも
    あわせて検討する。
    あわせて、**短時間にBLEスキャンを繰り返すとAndroidのスキャン制限に当たり`NOT_FOUND`が続く**
    ことも考慮する（2026-09-20の検証で、検証のために続けて操作した結果、数分にわたり
    `lock result=NOT_FOUND`が返り続けた。1分ほど間隔を空けると復帰した）。
    到達確認の回数を減らす方向の変更は、この制限を踏みにくくする効果もある。
    2026-09-21に実装した。`mobile.ble.SesameBleAttempt` / `SesameBleFailure`がBLEの結果を
    `NOT_REACHED`（圏外・権限なし）と`REACHED_BUT_FAILED`（見つかったが失敗）の2値へ落とし、
    `SesameBleReachability.record`が後者では到達実績を残す。連続失敗は
    `MAX_CONSECUTIVE_FAILURES`（3回）で打ち切り、実績の時刻は成功時のみ更新する。
  優先度: P3
  状態: 進行中
  担当: ユーザー
  完了条件: >-
    `CONNECTION_FAILED` / `LOGIN_FAILED`で失敗した直後の操作が、到達確認を待たずにBLEを再試行すること。
    `NOT_FOUND`（圏外）では従来どおりインターネット経由へ切り替わり、無駄なBLE試行を繰り返さないこと。
    判定のユニットテストがあること。
    2026-09-21に実装とユニットテスト・品質ゲートまで完了。
    実機（Pixel 8 Pro + 登録済みのSesame 5）で、接続に失敗した直後の操作がBLEを再試行すること
    （診断ログに`detail=CONNECTION_FAILED`または`detail=LOGIN_FAILED`の直後、次の操作でも
    `route=BLE`が出ること）の確認だけが未実施のまま残る（未確認）
  根拠: >-
    BL-189の再検証で観測した挙動で、BL-189の範囲（時間設計とアドレスキャッシュ）とは別の論点のため
    分離した。実装しなくてもインターネット経由で動作するため優先度はP3とする。
  依存: []

- id: BL-189
  区分: 性能
  タスク内容: >-
    BLE直接操作の経路選択の時間設計を、2026-09-20の実機検証（BL-165）の実測へ合わせ直す。
    実測ではBLE1往復が2.0〜3.5秒（Pixel 8 Pro + Sesame 5。間隔を空けた状態取得3回で
    2,437 / 2,992 / 3,140ms、施錠2,689ms、解錠3,918ms）で、
    `mobile.command.SesameDeviceCommandExecutorFactory.BLE_TIMEOUTS`の旧値
    （合計1,800ms / 探索900ms / 到達確認1,500ms）では毎回打ち切られ、
    `route=BLE op=STATUS result=NG`となってインターネット経由へ倒れていた。到達確認もSesameの
    真横で6回中2回しか成功せず、結果としてBLE経路は実運用で一度も使われていなかった
    （BL-165(6)〜(8)の「API呼び出し回数が増えない」も成立していない）。
    支配的なのは探索（スキャン）のため、(1) 見つけたBLEアドレスをuuidごとに保存する
    `SesameBleAddressCache`を追加し、(2) 接続手順を`SesameBleConnector`へ切り出して保存済み
    アドレスへの直接接続を先に試し、(3) 探索はWeb APIの通信と並行して走る到達確認へ寄せて
    その上限を1,500ms→4,000msへ広げ、(4) 利用者を待たせる経路の合計を1,800ms→2,600msへ広げた。
    あわせて到達確認の間隔（`SesameBleReachability.PROBE_INTERVAL_MILLIS`）を15分→5分、
    ウィジェットの受信上限（`WidgetCommandReceiver.WORK_TIMEOUT_MILLIS`）を8秒→9秒にした。
    同日の実機再検証で、**保存済みアドレスへの直接接続が毎回失敗する**ことが分かり追加で修正した。
    Sesame 5のBLEアドレス（例 `E9:38:...`）は先頭オクテットの上位2ビットが`11`の
    **static random address**で、`BluetoothAdapter.getRemoteDevice()`は公開アドレスとして扱うため
    接続要求が届かない。`getRemoteLeDevice(address, ADDRESS_TYPE_RANDOM)`（API 33以上）へ差し替え、
    API 33未満では保存済みアドレスを使わず探索へ倒すようにした。
    あわせて、どの段階で失敗したかを切り分けられるよう診断ログへ`detail=SUCCESS` /
    `NOT_FOUND` / `CONNECTION_FAILED` / `LOGIN_FAILED`を残すようにした。
    上限は再検証の結果さらに調整し、最終的に全体3,000ms / 探索1,200ms / 接続1,600ms /
    ログイン1,200ms / コマンド800ms / 到達確認4,000msとした
    （BLEが倒れたときの最悪値が3.0+6.0=9.0秒で、ウィジェットの上限と同じところに収まる）。
    2026-09-20に実装・品質ゲート・実機での再検証まで完了。
  優先度: P2
  状態: 進行中
  担当: ユーザー
  完了条件: >-
    実機（Pixel 8 Pro + 登録済みのSesame 5）で、Sesameの近くにいるときの操作が実際にBLE経路で
    成功し（診断ログが`route=BLE ... result=OK`となり、設定画面の「今月のAPI呼び出し回数」が
    増えないこと）、ウィジェットのタップが打ち切られずに結果が出ること。離れた場所では従来どおり
    インターネット経由で成功すること。
    2026-09-20に、BLE経路が実際に選ばれて成功すること（`detail=SUCCESS` / `result=OK`）と、
    その操作でAPI呼び出し回数が増えないこと（56→56）を確認済み。成功率は上限調整後の実測で3回中2回
    （残り1回は`NOT_FOUND`でインターネット経由へフォールバックし、操作自体は成功）。
    ウィジェットのタップが打ち切られないことの確認だけが未実施のまま残る（未確認）
  根拠: >-
    旧値は実測に基づかない見積もりで、BL-165の完了条件が「実測値が設計値の妥当性を決める」と
    していた箇所そのものにあたる。BLEアドレスは端末・起動ごとに変わりうるため保存しない方針だったが、
    変わっていれば直接接続が失敗して探索へ戻るだけなので、探索を省ける利益のほうが大きいと判断した
    （2026-09-20のユーザー指示「BLEでの接続をある程度諦めない形で」に対応する）。
  依存: []

- id: BL-182
  区分: 人手検証
  タスク内容: >-
    BL-173〜BL-181の表示・操作を実機で確認する。確認する内容は、
    (1) ホーム画面ウィジェットを2マス×1マスまで縮められ、それ未満へは縮まらないこと、
    (2) 2x1で左に「デバイス名」と「◀ ▶」、右にアイコンと状態文言が省略・見切れなく出ること、
    (3) ◀ ▶ で対象デバイスが選択画面と同じ順に切り替わり、切り替え後も表示・タップ操作が正しいこと、
    (4) 4x2（FULL）は従来どおり「変更」で選択画面が開くこと、
    (5) 経路アイコンが🔗（Bluetooth）／🌐（インターネット）でTile・Complication・
    ウォッチの状態一覧に出ること、
    (6) スマホのアプリ画面とウィジェットのFULLで、経路がMaterialのベクターアイコンで出ること、
    (7) スマホのアプリ画面が上部バー＋カード一覧＋追加ボタンの構成になり、
    1行で収まらない情報が無いこと、
    (8) 追加・編集が全画面ダイアログで開き、保存後に一覧へ反映されること、
    (9) 削除で確認ダイアログが出て、キャンセルすると消えないこと、
    (10) 上部バーのメニューからヘルプと接続の設定がダイアログで開くこと、
    (11) 端末をダークテーマにするとアプリ画面が暗色になり、
    ステータスバーの時刻・電池が判読できること。
    実資格情報は不要で、ダミー資格情報とモックAPI（`-PsesameApiBaseUrl`）で代替できる
    （手順は Skill `realmachine-verification`）。BLE経由（🔗）の表示だけは実Sesameデバイスが要るため
    BL-165に含める。
    2026-09-20にClaude Codeがエミュレータ（`nocompanion` = Pixel 6相当 / Android 15）で
    (1)〜(4)(6)〜(11)を検証済み（ユーザー指示により実機ではなくエミュレータ単独で実施）。
    内訳はDESIGN.md「エミュレータ検証（BL-182 / BL-183 / BL-184 / BL-185 / BL-186、2026-09-20）」。
    検出した不具合3件はBL-183〜BL-185として起票し、同日中に修正と再確認を終えて完了している。
    あわせて2マス×1マスを初期サイズとして選べるようにした（BL-186）。
    2026-09-20に(5)のうち🌐（インターネット経由）をPixel Watch 2の実機で確認した。
    Tileの右チップ（「施錠中 / 🌐6分前 / タップで解錠」）と、ウォッチのアプリ本体の状態一覧
    （2台とも「🔒施錠中 🔋100%」「🌐n分前」）のどちらも省略・見切れが無い。
    同日夜、BL-189の修正後にBLE経路が選ばれるようになったため、🔗（Bluetooth経由）も確認した。
    ウォッチの状態一覧で「玄関上＝🔗たった今」「玄関下＝🌐1時間前」が同時に並び、Tileの右チップも
    「施錠中 / 🔗たった今 / タップで解錠」で省略・見切れが無い。
    残るのはComplicationの経路アイコンだけで、利用者の文字盤へデバッグ版のデータソースを
    割り当てる必要がある。
  優先度: P2
  状態: 進行中
  担当: ユーザー
  完了条件: >-
    上記(1)〜(11)をPixel 8 Pro（＋Tile・Complicationの確認にPixel Watch 2）で確認し、
    表示の省略・見切れが無いこと。収まらない箇所があれば不具合として起票されていること。
    (1)〜(4)(6)〜(11)は2026-09-20にエミュレータで、(5)のうちTileと状態一覧の🌐・🔗は同日に実機
    （Pixel Watch 2）で確認済み。Complicationの経路アイコンだけが未確認のまま残る
  根拠: >-
    表示の崩れとリサイズの下限はユニットテストでは検出できず、実機の描画とランチャーの挙動が
    必要なため人手検証とする。文言・レイアウト判定のロジック自体は
    `SesameWidgetLayout` / `WidgetDeviceCycle` / `SesameRouteLabel` のユニットテストで検証済み
  依存: []

- id: BL-172
  区分: 人手検証
  タスク内容: >-
    経路表示と状態表示（BL-166〜BL-171）を実機で確認する。とくに表示の収まりを見る。
    Tile・Complication・ウィジェットの「最終取得時刻」の行へ経路アイコン（📶/☁）を前置し、
    ウィジェットには電池残量も同じ行へ足したため、過去に文言が収まらず省略された事例
    （BL-102 / BL-104 / BL-158）と同じ問題が起きないかを確認する必要がある。
    確認する内容は、(1) Tileの右チップで「📶23時間前」「☁認証エラー」が省略・見切れなく出ること、
    (2) ウィジェットのFULLで「📶3分前 🔋85%」が2行以内に収まり、最小サイズ（4x1相当）へ縮めても
    操作文言が見切れないこと、(3) COMPACTでは従来どおりアイコンと状態文言だけであること、
    (4) スマホのデバイス一覧に電池・施錠状態・最終取得・経路が1行で出ること、
    (5) ウォッチのアプリ本体が状態一覧になり、スクロールして全デバイスを見られること、
    (6) 経路の方針を「常にインターネット経由」にするとBLEを試さなくなり、表示も☁になること、
    (7) BLEに届かずフォールバックしたときにスマホでトーストが出ること
    （ウィジェット操作・ウォッチ操作の双方で確認する）。
    (8) ウォッチのアプリを開いても「今月のAPI呼び出し回数」が増えないこと
    （この画面から状態取得のリクエストを送らない設計の確認）。
    (9) スマホのデバイス一覧に角度が出ること、ウォッチの状態一覧には角度が出ないこと。
    フォールバックの再現は、Bluetoothをオフにする、またはSesameから離れることで行える。
    電池残量の値が実際のセサミと一致するかは、実資格情報を要するためBL-165で確認する。
    2026-09-20にClaude Codeがadb経由で(1)〜(6)(8)(9)を検証済み（Pixel 8 Pro + Pixel Watch 2、
    デバッグ版とダミー資格情報4台とモックAPI）。表示の省略・見切れは無く、不具合は検出していない
    （内訳はDESIGN.md「実機検証（BL-172 / BL-165、2026-09-20）」）。
    2026-09-20に(7)を実Sesameデバイスで検証し、**トーストが表示されないこと**を確認した。
    フォールバック自体は起きており（`route=BLE op=STATUS result=NG`）、トーストも発行されているが、
    `NotificationService: Suppressing toast from package ... by user request.`としてシステムに
    抑止される。アプリを前景にすると表示されるため、原因は通知が無効な端末での背景からの
    トースト抑止（Android 12以降）。対応はBL-190で行い、通知へ置き換えた。
    残るのは経路アイコン🔗を伴う表示で、BLE経路が実運用で選ばれるようになってから
    （BL-189の修正後）でなければ再現できない。
  優先度: P3
  状態: 進行中
  担当: ユーザー
  完了条件: >-
    上記(1)〜(9)を実機（Pixel 8 Pro + Pixel Watch 2 + 登録済みのSesame 5）で確認し、
    表示の省略・見切れが無いこと。収まらない箇所があれば不具合として起票されていること。
    (1)〜(6)(8)(9)は2026-09-20に確認済み。(7)は同日に検証し、トーストが表示されない不具合を
    検出してBL-190として対応した（再確認はBL-190の完了条件に含める）。
    🔗アイコンを伴う表示も同日、BL-189の修正後に確認した（BL-182(5)へ記載）
  根拠: >-
    表示の崩れはユニットテストでは検出できず、実機の描画が必要なため人手検証とする。
    文言の決定ロジック自体はcore.displayのユニットテストで検証済み。
    電池残量の値が実際のSesameと一致するかの確認はBL-165に含める（実資格情報を要するため）。
  依存: []

- id: BL-154
  区分: 人手検証
  タスク内容: >-
    BLE直接操作（BL-152）の配信にあたり、Google Playのデータセーフティ申告・権限の用途説明・
    ストア掲載情報を更新する。追加される権限はBLUETOOTH_SCAN / BLUETOOTH_CONNECT
    （API 31以上）とACCESS_FINE_LOCATION（`android:maxSdkVersion="30"`で旧端末限定）。
    位置情報権限は旧端末限定でも申告の対象になりうるため、Play Consoleの質問に沿って
    「位置情報を収集・共有しない（BLEスキャンのためだけに宣言している）」ことを説明する。
    docs/USER_GUIDE.md と docs/RELEASE_NOTES.md への記載は2026-09-19に完了済みのため、
    残りは Play Console 上の操作と docs/store/STORE_LISTING.md の更新。
    2026-09-20のBL-190で POST_NOTIFICATIONS も追加したため、こちらも申告・説明の対象に含める
    （経路が切り替わったことを知らせる通知にだけ使い、収集・共有は行わない）。
  優先度: P3
  状態: 未着手
  担当: ユーザー
  完了条件: >-
    データセーフティ申告と権限の用途説明が更新され、審査を通過して配信されていること。
    利用者向けドキュメントにBLE経由で動作する条件（スマートフォンがSesameの電波圏内にあること）が
    記載されていること
  根拠: >-
    Play Consoleの操作を伴うため自律ループ実行モードでは実行できない
    （rules/guardrails-unified.v1.md セクション12.2）。DESIGN.md「BLE直接操作の併用方針」の
    段階5に対応する。BL-152・BL-153の実装は2026-09-19に完了しており、依存は解消している
    （申告の対象となる権限は mobile/src/main/AndroidManifest.xml で確定済み）。
  依存: []

- id: BL-165
  区分: 人手検証
  タスク内容: >-
    BL-151〜BL-153で実装したBLE直接操作を、デバッグビルドと実Sesameデバイスで検証する。
    `-PsesameApiBaseUrl`に相当するモック差し替えがBLEには存在せず、実機でしか確認できない。
    デバッグビルドにだけ存在するブロードキャスト受信口から実行する（リリースビルドには含まれない）。

    ```
    adb shell am broadcast -a com.sesamiwear.mobile.debug.BLE       -n com.sesamiwear.mobile.debug/com.sesamiwear.mobile.ble.SesameBleDebugReceiver       --es uuid <uuid> --es op status
    ```

    `--es op`は`status` / `lock` / `unlock`。結果は`adb logcat -s SesameBleDebug`で読む。
    secretKeyは端末内の暗号化ストアから読むため、コマンドラインへは渡さない。
    確認する内容は、(1) `op=status`で`result=SUCCESS`となり`isInLockRange`・`position`・
    `batteryPercentage`が公式アプリの表示と一致すること、(2) `op=lock` / `op=unlock`で実際に
    施錠/解錠されること、(3) スマートフォンの機内モード（Wi-Fi・モバイル通信オフ、Bluetoothオン）でも
    成功し、クラウドへ接続していないこと、(4) 権限を拒否した状態では`result=PERMISSION_DENIED`に
    なるだけで、従来どおりWeb API経由の施錠/解錠・状態取得が動くこと。
    あわせて、スキャンから応答までの所要時間を計測する。BL-152の経路選択は全体で1,800ms、
    うち探索・接続に1,600msしか割けない設計値のため（DESIGN.md「経路選択の実装」）、
    実測値が設計値の妥当性を決める。1,800msに収まらない場合は設計値とTTL
    （REACHABLE_TTL_MILLIS 30分 / PROBE_INTERVAL_MILLIS 15分）を見直す。

    経路選択（BL-152）と権限UI（BL-153）についても、通常の操作で次を確認する。
    (5) 資格情報設定画面に「Bluetooth：未許可（インターネット経由で動作中）」が出ること、
    「設定」から説明ダイアログが開き、許可すると表示が「許可済み」へ変わること。
    拒否した場合は「端末の設定を開く」導線へ切り替わること。
    (6) 権限を許可した状態でSesameの近くにいるとき、ウィジェット／Tileの操作が成功し、
    設定画面の「今月のAPI呼び出し回数」が増えないこと（BLE経路を通った証拠になる）。
    (7) Sesameから離れた場所では同じ操作が成功し、呼び出し回数が増えること。
    (8) 近くへ戻ったあと、最初の1回はインターネット経由でも、その次からは回数が増えなくなること
    （並行して行う到達確認が効いていること）。
    (9) ウィジェットのタップが打ち切られないこと（8秒以内に結果が出ること）。
    経路の判別は `adb logcat -s SesameApiFailureLog` の `route=BLE` の行でも確認できる。
    2026-09-20にClaude Codeがadb経由で、実Sesameデバイスを要しない範囲を検証済み。
    (4)の権限拒否時は`status result=PERMISSION_DENIED`となり、同じ状態でWeb API経由の状態取得が
    成功することを確認した。(5)のうち資格情報設定画面の表示（未許可／許可済みの文言と「設定」導線）も
    確認した（説明ダイアログからの許可操作は`pm grant`で代替したため未確認）。
    BL-172から移管した「BLEで届かなかったときのトースト」と「経路アイコン🔗を伴う表示」も、
    ここで確認する（内訳はDESIGN.md「実機検証（BL-172 / BL-165、2026-09-20）」）。
    同日夜に実Sesameデバイスを伴う範囲を検証した（内訳はDESIGN.md
    「実機検証（BL-165 / BL-182、2026-09-20）」）。(1)は`op=status`が`result=SUCCESS`となり
    `isInLockRange=true` / `position=-180` / `batteryPercentage=100`を取得、
    (2)は`op=unlock`（3,918ms）→`position=-270`、`op=lock`（2,689ms）→`position=-180`で
    実際に施錠/解錠できた。所要時間は状態取得が2,437 / 2,992 / 3,140ms（間隔15秒）で、
    **連続実行すると`CONNECTION_FAILED` / `NOT_FOUND`になる**。
    (5)の説明ダイアログからの権限許可も確認した（`BLUETOOTH_SCAN` / `BLUETOOTH_CONNECT`が
    `granted=true, USER_SET`）。
    (6)〜(8)は**成立しなかった**。設計値（合計1,800ms / 到達確認1,500ms）が実測に対して短く、
    到達確認はSesameの真横でも6回中2回しか成功せず、成立しても本番の操作は打ち切られて
    毎回インターネット経由へ倒れる。設計値の見直しはBL-189で実施した。
    BLEがAPI呼び出し回数を消費しないことは確認済み（BLE操作4回の前後でカウントは52のまま。
    実装上も`recordApiCall`はWeb API経路にだけ配線されている）。
    (3)の機内モードは、BL-189の修正後にUSB接続で検証した。機内モードをオンにしただけではWi-Fiが
    残るため、あわせてWi-Fiも切り`Active default network: none`（`ping`も到達不可）にしたうえで
    `op=unlock`が`result=SUCCESS`となり、API呼び出し回数は67のまま増えなかった。
    施錠して戻した状態（`isInLockRange=true` / `position=-181`）も確認済み。
  優先度: P3
  状態: 進行中
  担当: ユーザー
  完了条件: >-
    上記(1)〜(9)を実機（Pixel 8 Pro + Pixel Watch 2 + 登録済みのSesame 5）で確認し、
    スキャンから応答までの所要時間の実測値と、設計値を見直したかどうかがDESIGN.mdへ
    記載されていること。(1)(2)(4)(5)と所要時間の実測は2026-09-20に確認・記載済みで、
    設計値の見直しはBL-189として実施した。(3)の機内モードと(6)〜(8)の経路切り替え、
    🔗アイコンの表示は、BL-189の修正後に同日確認済み。
    (9)のウィジェットのタップが打ち切られないことだけが未確認のまま残る。
    (1)の「公式アプリの表示と一致すること」も、電池残量100%・施錠中の突き合わせが未実施
  根拠: >-
    実資格情報（secretKey）と実Sesameデバイスを要するため、自律ループ実行モードの完了判定から
    除外する（rules/guardrails-unified.v1.md 12.5）。BL-151〜BL-153の完了条件のうち実機を要する
    部分を切り出したもので、実装そのものは2026-09-19に完了している。BLEには
    `-PsesameApiBaseUrl` に相当するモック差し替えが存在せず、デモモードでも代替できない。
  依存: []

- id: BL-149
  区分: 人手検証
  タスク内容: >-
    BL-142で追加した「最後に状態を取得した時刻」の表示が、Tile・Complication・ホーム画面
    ウィジェットで崩れないことを実機で確認する。とくにTileの右チップは、アイコン・状態文言・
    最終取得時刻・操作文言の4要素を縦に並べる構成になったため、円形画面のセーフエリア
    （内接正方形）で末尾省略や見切れが起きないかを見る。文言が最も長くなるのは
    「23時間前」（5文字）と「未取得」で、操作文言が併記される施錠中/解錠中/一部解錠の3状態が
    最も詰まる。過去にTileの文言がチップ幅に収まらず省略された事例がある（BL-102 / BL-104）。
    Complicationは`LONG_TEXT`枠で「玄関 施錠（3分前）」が収まるか、`SHORT_TEXT`枠の表示が
    従来どおりであることを確認する。BL-140で追加した失敗時の文言（Tileは「認証エラー」「通信エラー」、
    ウィジェットは「認証エラー（設定を確認）」「通信エラー（電波状況を確認）」）も同じ位置に出るため、
    こちらも省略・見切れが無いことを確認する。ウィジェットの文言は12文字あり2行での表示になる。
    失敗の再現は、モックAPI（`-PsesameApiBaseUrl`）に403を返させる、または接続先を存在しない
    ホストへ向けることで、実資格情報を使わずに行える。
    あわせてBL-128で追加したウィジェットのサイズ別レイアウトを確認する。ホーム画面へ追加した直後は
    4x2相当であること、1マス（1x1）まで縮められること、縮小時にアイコンと状態文言だけの表示へ
    切り替わり文字が欠けないこと、縮小時もタップで施錠/解錠できること、長押しメニューから対象
    デバイスを変更できること。Android 11以下の端末があれば、既定の配置サイズが1マスへ縮んでいない
    ことも確認する（`minWidth`/`minHeight`は据え置いているが、実機での確認は未実施）。
    さらにBL-129で追加したウィジェット操作の振動を確認する。施錠/解錠の成功で短い振動2回、
    失敗で長い振動1回が鳴ること、2秒以内の連打（重複として無視された場合）とデバイス名タップの
    状態取得では鳴らないこと、ウォッチのタイルと手触りが揃っていること。
    BL-147で追加した「今月のAPI呼び出し回数」が資格情報設定画面の見出し直下に出ること、
    施錠/解錠や状態取得のたびに増えること（画面を開き直すと反映される）、デモ用デバイスの操作や
    連打として無視された操作では増えないことも確認する。
    BL-148で追加した状態取得の連打抑止（同一デバイスへの2秒以内の再タップを無視する）も、
    呼び出し回数が増えないことで確認できる。あわせて、自動取得を廃止した結果としてTileを開いても
    リクエストが飛ばないこと（表示が更新されないこと）と、デバイス名タップでのみ更新されることを
    確認する。実資格情報を使わずに済む範囲は、デモモードとモックAPI（`-PsesameApiBaseUrl`）で
    代替できる（手順は Skill `realmachine-verification`）。
    2026-09-18にClaude Codeがadb経由で大半を検証済み（Pixel 8 Pro + Pixel Watch 2、デバッグ版と
    ダミー資格情報2台とモックAPI）。TileとウィジェットのBL-142・BL-140・BL-128・BL-147・BL-148の
    表示と挙動は確認できた（内訳はDESIGN.md「実機検証（BL-149、2026-09-18）」）。
    Complicationは`SHORT_TEXT`枠の表示まで確認した。検出した不具合はBL-156〜BL-160として
    起票し、同日中に修正と実機での再確認を終えて完了済み（DESIGN.md
    「実機検証（BL-156 / BL-157 / BL-158 / BL-159 / BL-160、2026-09-18）」）。
    残りはComplicationの`LONG_TEXT`枠の表示確認（該当枠を持つ文字盤へ割り当てられて
    いない）と、Android 11以下の端末での既定の配置サイズの確認（該当端末が手元に無い）。
    2026-09-19 に、`LONG_TEXT` 枠の追試は不要とユーザーが判断した。どちらも未確認のまま
    0.12.0 を配信する（BL-155）。
  優先度: P2
  状態: 進行中
  担当: ユーザー
  完了条件: >-
    Pixel Watch 2 のTileとComplication、Pixel 8 Pro のホーム画面ウィジェットで、
    最終取得時刻の表示が省略・見切れなく表示されること。Tileを開いただけでは状態が更新されず、
    デバイス名のタップで更新されることを確認していること。
    Tileとウィジェットと`SHORT_TEXT`枠のComplicationは2026-09-18に確認済みで、
    Complicationの`LONG_TEXT`枠の表示確認とAndroid 11以下での既定の配置サイズの確認が
    未実施のまま残る（未確認）
  根拠: >-
    表示の崩れはユニットテストでは検出できず、実機またはエミュレータの描画が必要なため
    人手検証とする。文言の決定ロジック自体は`core.display.SesameStatusFreshness`の
    ユニットテストで検証済み。adb経由で検証できる範囲は自動化を試みてよい
    （CLAUDE.md「品質ゲート定義」の人手検証に関する補足）。
  依存: []

- id: BL-106
  区分: 人手検証
  タスク内容: Google Playの製品版公開に必要なクローズドテスト（12人以上が14日間連続でオプトイン）を
    実施し、製品版へのアクセスを申請する。テスター管理はメールアドレスのリストではなく
    Googleグループ方式を採用し、参加者が自分でグループへ参加してオプトインする自己完結フローに
    する（ユーザーがメールアドレスを収集・保管しない方針）。手順の詳細と現在地は
    docs/store/PLAY_CONSOLE_STEPS.local.md の「次回の再開ポイント」フェーズ8（クローズドテスト）を
    参照する。2026-09-14に0.10.0が両トラックで公開中となり、2026-09-15にQiita記事とXで募集を開始した。
    2026-09-18時点でオプトイン中のテスターは1人で、12人には届いていない（Play Consoleのダッシュボードで確認）。
    同日に0.11.0が両トラックで公開され、docs/CLOSED_TEST.mdの参加条件が変わった
    （スマートウォッチが無くてもホーム画面ウィジェットで試せる）ため、募集先（Qiita記事・X）への
    反映が必要。反映済みかは未確認
  優先度: P2
  状態: 進行中
  担当: ユーザー
  完了条件: クローズドテストで12人以上が14日間連続でオプトインした状態を満たし、製品版への
    アクセス申請が承認される
  根拠: Claudeによる代替実施は不可。Play ConsoleとGoogleグループはブラウザ専用の操作であり、
    テスターの募集も外部への発信にあたるため自律ループ実行モードでは実行できない
    （rules/guardrails-unified.v1.md セクション12.2）
  依存: []


```
<!-- COPILOT_RECORDS:END -->
