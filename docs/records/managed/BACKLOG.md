<!-- markdownlint-disable-file MD041 -->
<!-- Copilot専用未対応事項記録ファイル（ユーザ編集禁止） -->
<!-- このファイルはCopilotがプロンプト指示の処理実行時のみ自動更新します。 -->
<!-- schema: records.backlog.v1 -->

<!-- COPILOT_RECORDS:BEGIN -->
```yaml
- id: BL-145
  区分: 機能追加
  タスク内容: >-
    Web API以外の経路として、BLE（Bluetooth Low Energy）での直接操作を併用できるかを検討する。
    Web APIには月間リクエスト上限があり、到達するとアプリ全体が機能停止する（BL-141）。
    BLEはクラウドを経由しないため上限の影響を受けず、Hub 3が無くても動く一方、Sesameの電波圏内でしか
    使えない。「代替」ではなく「圏内ではBLE、圏外ではWeb API」という併用の可否として評価する。
    2026-09-18に公開リポジトリを調査し、実現可能性は高いという心証を得た（詳細は下記）。
    残る論点は、鍵の同一性の実証と、アーキテクチャ・配布への影響の評価。
    論点1（鍵の同一性、最重要）。非公式実装は一様に「16進数32文字のsecret key」だけでSesame 5を
    BLE操作しており、meronepy/gomalockのREADMEは、このsecret keyの入手元として
    SESAME BizとQRコードの両方を挙げている。本アプリがbiz.candyhouse.coから取得して保持している
    secretKey（16進数32文字、BL-058）と同一である可能性が高いが、実機で未実証。
    なお現在のヘルプ文言「secretKeyは16進数32文字です。Sesameアプリの『鍵をシェア』QRコードの値では
    ありません」は、QRのsk値がbase64かつ先頭1バイトがモデル番号であることに由来する表現であり、
    同一性が実証された場合は誤解を招かないよう見直す（ヘルプの文言はBL-144で
    `mobile.help.HelpContent.credentials`に残っている）。
    論点2（クラウド依存）。公式SDKのREADMEはAmplify（AWS Cognito / API）の初期化を求めているが、
    これは登録・クラウド機能向けであり、非公式実装はいずれもクラウドに一切接続せずBLEのみで
    施錠/解錠まで到達している。既登録デバイスの操作にAWS設定は不要と考えられるが、公式SDKを
    そのまま使う場合に初期化で要求されるかは未確認。
    論点3（secretKeyの保持場所）。現行方針はsecretKeyをmobileのみが持ち、wearには持たせない
    （DESIGN.md「アーキテクチャ方針」）。wearが直接BLE操作する構成はこの方針の変更を伴うため、
    既定はmobileがBLEを担い、wearは従来どおりData Layer経由でコマンドを送る構成とする。
    この場合スマホがSesameの電波圏内に無いと動かないため、経路の優先順位と切り替え条件を定義する。
    論点4（権限と審査）。追加される権限はACCESS_FINE_LOCATION / BLUETOOTH_SCAN / BLUETOOTH_CONNECTと
    uses-feature android.hardware.bluetooth_le。位置情報権限の追加はGoogle Playのデータセーフティ
    申告と権限の用途説明に影響するため、審査上の追加作業を洗い出す。
    論点5（実装方式とサイズ）。公式SDKをJitPackで取り込む、公式SDKのBLE部分を参考に自前実装する、
    のいずれかを選ぶ。coreはAndroid非依存の制約があるためBLE実装はcoreへ置けずmobile側の新パッケージに
    なるが、AES-CMACはcoreに実装済み（com.sesamiwear.core.crypto.AesCmac）で流用できる。
    -PsesameApiBaseUrlのようなモック差し替えがBLEには無く、検証は実機必須になる。
  優先度: P2
  状態: 未着手
  担当: AIエージェント
  完了条件: >-
    論点1〜5それぞれについて結論（可否と根拠）がDESIGN.mdへ記載されていること。とくに論点1は、
    保持中のsecretKeyでBLE接続と状態取得ができたかどうかを実機で確認した結果として記載すること。
    採用する場合は経路の優先順位・切り替え条件・段階的な移行案が、見送る場合はその理由が記載され、
    実装作業が別項目として起票されていること
  根拠: >-
    本項目は調査と採否判断までを範囲とし、実装は含めない。BLEは上限問題に対する構造的な
    解決策になりうる。短期的なリクエスト数削減（自動状態取得の廃止）はBL-142で対応済みで、
    本項目の結論を待たずに進めた（依存は張らない）。
    論点1の実証は実機のSesameとsecretKeyを要するため、当該ステップのみ人手検証として扱う。
  依存: []

- id: BL-146
  区分: 機能追加
  タスク内容: >-
    BL-145の検討で参照する外部実装の調査結果。2026-09-18時点の調査で見つかった参照先を、
    採否判断とその後の実装で再調査せずに済むよう記録しておくための項目。
    公式実装。CANDY-HOUSE/SesameSDK_Android_with_DemoApp（Kotlin、MITライセンス、2026-09-14時点で
    更新継続、minSdk 24 / JDK 17 / Android SDK 36、JitPack配布
    com.github.CANDY-HOUSE.SesameSDK_Android_with_DemoApp:sesame-sdk:<version>）。
    BLE実装は sesame-sdk/src/main/java/co/candyhouse/sesame/ble/os3/ にあり、Sesame 5は
    CHSesame5Device.kt が担当する。プロトコル定義は同 ble/SesameProtocols.kt、AES-CMACは
    同 utils/aescmac/ 配下。VALIARK-jp/Pedal_Share は、このSDKを android/sesame-sdk モジュールとして
    自アプリへ同梱した先行事例。
    非公式実装。いずれもクラウドへ接続せずBLEのみで動作する。
    meronepy/gomalock（Python、MIT、2026-08-25、Sesame 5 / 5 Pro / 5 USAの施錠・解錠・トグル、
    角度・電池・オートロックの取得と変更、スキャン、新規登録に対応）が最も読みやすく、
    必要な資格情報はBLEアドレスと16進数32文字のsecret keyのみ。
    homy-newfs8/libsesame3bt-core（C++、MIT、2026-08-23）はBLE接続部を外に出したメッセージ処理の
    実装で、Sesame 5ではset_keysの公開鍵を空文字にしてsecretだけを渡す。サービスUUIDは
    0000fd81-0000-1000-8000-00805f9b34fb、Tx/Rxの各キャラクタリスティック経由で通信する。
    bingxyz/ha-sesame-ble（Python、MIT、Home Assistant統合、gomalockを利用、SESAME 5 Proの実機で
    検証済み）、lanpili/ha-sesame-local、Khronos31/home-assistant-candy-house-ble、
    zunda-pixel/sesame-swift（Swift、Apache-2.0、Remote/Localの両クライアント）も参照先になる。
    これらのライセンスはMITまたはApache-2.0で、参照・流用の障害は無い。
  優先度: P3
  状態: 完了
  担当: AIエージェント
  完了条件: >-
    BL-145の検討に必要な外部実装の所在・ライセンス・必要な資格情報・プロトコルの入口
    （サービスUUIDと実装ファイルのパス）が記録されていること
  根拠: >-
    調査結果そのものを残す項目のため、起票時点で完了状態とする。BL-145の着手時に参照し、
    BL-145の完了時に本項目も削除する。本文の記述はいずれも各リポジトリのREADMEとGitHub APIで
    確認した2026-09-18時点の事実で、実機での検証は行っていない。
  依存: []

- id: BL-149
  区分: 人手検証
  タスク内容: >-
    BL-142で追加した「最後に状態を取得した時刻」の表示が、Tile・Complication・ホーム画面
    ウィジェットで崩れないことを実機で確認する。とくにTileの右チップは、アイコン・状態文言・
    最終取得時刻・操作文言の4要素を縦に並べる構成になったため、円形画面のセーフエリア
    （内接正方形）で末尾省略や見切れが起きないかを見る。文言が最も長くなるのは
    「23時間前」（5文字）と「未取得」で、操作文言が併記される施錠中/解錠中/一部解錠の3状態が
    最も詰まる。過去にTileの文言がチップ幅に収まらず省略された事例がある（BL-102 / BL-104）。
    Complicationは`LONG_TEXT`枠で「玄関 施錠（3分前）」が収まるか、`SHORT_TEXT`枠の表示が
    従来どおりであることを確認する。BL-140で追加した失敗時の文言（Tileは「認証エラー」「通信エラー」、
    ウィジェットは「認証エラー（設定を確認）」「通信エラー（電波状況を確認）」）も同じ位置に出るため、
    こちらも省略・見切れが無いことを確認する。ウィジェットの文言は12文字あり2行での表示になる。
    失敗の再現は、モックAPI（`-PsesameApiBaseUrl`）に403を返させる、または接続先を存在しない
    ホストへ向けることで、実資格情報を使わずに行える。あわせて、自動取得を廃止した結果としてTileを開いても
    リクエストが飛ばないこと（表示が更新されないこと）と、デバイス名タップでのみ更新されることを
    確認する。実資格情報を使わずに済む範囲は、デモモードとモックAPI（`-PsesameApiBaseUrl`）で
    代替できる（手順は Skill `realmachine-verification`）。
  優先度: P2
  状態: 未着手
  担当: ユーザー
  完了条件: >-
    Pixel Watch 2 のTileとComplication、Pixel 8 Pro のホーム画面ウィジェットで、
    最終取得時刻の表示が省略・見切れなく表示されること。Tileを開いただけでは状態が更新されず、
    デバイス名のタップで更新されることを確認していること
  根拠: >-
    表示の崩れはユニットテストでは検出できず、実機またはエミュレータの描画が必要なため
    人手検証とする。文言の決定ロジック自体は`core.display.SesameStatusFreshness`の
    ユニットテストで検証済み。adb経由で検証できる範囲は自動化を試みてよい
    （CLAUDE.md「品質ゲート定義」の人手検証に関する補足）。
  依存: []

- id: BL-148
  区分: 性能
  タスク内容: >-
    状態取得（`PATH_STATUS_REQUEST` / ウィジェットのデバイス名タップ）の連打を抑止する。
    施錠/解錠は`mobile.messaging.CommandDebouncer`が同一uuidへの2秒以内の重複を無視するが、
    状態取得は対象外のため、Tileのデバイス名チップやウィジェットのデバイス名を連打すると
    その回数ぶんSesame APIのGETが飛ぶ。BL-142で自動取得を廃止し、消費が利用者のタップ回数と
    等しくなったため、誤タップ・連打が月間リクエスト上限（BL-141）へ直接効くようになった。
    `SesameDeviceCommandExecutor.refreshStatus`へ、uuid単位の最小取得間隔を入れる。
    「全デバイス」対象のタップで登録台数ぶん飛ぶのは意図した動作のため抑止の対象外とする
    （uuidが異なるため同一uuidの重複には当たらない）。
  優先度: P3
  状態: 未着手
  担当: AIエージェント
  完了条件: >-
    同一uuidへの状態取得を短時間に繰り返しても、設定した最小間隔より多くAPIが呼ばれないことが
    ユニットテストで検証されていること。間隔の値と、その値にした根拠がDESIGN.mdへ記載されていること
  根拠: >-
    BL-142で「フルセット」として挙げた抑制策のうち、(3)mobile側でのuuid単位の最小取得間隔を
    分離したもの。自動取得の廃止により抑制対象が「自動取得」から「利用者の連打」へ変わったため、
    1ブランチ1目的の原則に沿って別項目とした（2026-09-18、ユーザー確認済み）。
    `CommandDebouncer`の2秒という値は連打防止のためのもので、状態取得にも同程度でよいかは要検討。
  依存: []

- id: BL-147
  区分: UX改善
  タスク内容: >-
    Sesame Web APIの月間リクエスト消費回数をアプリ側でカウントし、利用者が確認できるようにする。
    2026-09-18に上限（利用者のPlay版環境で1000回）へ到達し、状態取得・施錠/解錠のすべてが
    HTTP 403で失敗する事象が発生した（BL-141）。BL-142で自動取得を廃止して消費は利用者のタップ分
    だけになったが、利用者が「今月あと何回使えるか」を知る手段はアプリ内に無く、
    SESAME Bizのサイトを見に行くしかない。
    mobile側でAPI呼び出し回数を月単位（暦月。カウンタは月が変わったらリセット）で数え、
    資格情報設定画面またはヘルプから確認できるようにする。カウントはアプリ自身の呼び出し回数で
    あり、Sesame純正アプリなど他経路の消費は含まないため、あくまで目安である旨を明示する。
    上限値はアカウントのプランによって変わる可能性があり、アプリからは取得できないため、
    既定値（1000）を持ちつつ利用者が変更できるようにするかは検討する。
    月間上限の存在そのものの周知はBL-144で対応済み（アプリのヘルプ「APIのリクエスト回数の上限」、
    docs/USER_GUIDE.md、docs/SUPPORT.md、README.md）。本項目は実測値の提示に絞る。
  優先度: P3
  状態: 未着手
  担当: AIエージェント
  完了条件: >-
    今月のAPI呼び出し回数がアプリ内で確認できること。カウントと月替わりのリセットが
    Android非依存のクラスで行われ、ユニットテストで検証されていること。
    表示が目安であること（他経路の消費を含まないこと）が画面上の文言で分かること
  根拠: >-
    BL-142で「フルセット」として挙げた抑制策のうち、(4)月間消費回数の把握を分離したもの。
    自動取得を廃止したことで「上限接近時に自動取得を止める」という当初の目的は対象が消えたため、
    残る価値である「利用者が消費状況を把握できること」に絞って再定義した
    （2026-09-18、ユーザー確認済み）。BL-144で追加したヘルプ項目「APIのリクエスト回数の上限」と
    説明が重なるため、実測値をそこへ足すのか別の場所に出すのかを実装時に決める。
  依存: []

- id: BL-141
  区分: 人手検証
  タスク内容: >-
    Sesame APIの月間リクエスト上限（1000回）到達を解消し、状態取得と施錠/解錠が回復することを
    確認する。2026-09-18のPixel 8 Pro（Play版0.11.0）での調査で、状態取得が
    `https://app.candyhouse.co/api/sesame2/{uuid}`に対してHTTP 403
    （本文は`{"Message":"User is not authorized to access this resource with an explicit deny in
    an identity-based policy"}`）を返すことを確認した。この応答は、出鱈目なapikeyで同エンドポイントを
    叩いたときの応答と完全に一致する（ヘッダー無しの場合は401）。uuidの形式は36文字・大文字で正常、
    通信・権限・タイムアウト・R8による難読化はいずれも原因ではないことを切り分け済み。
    その後、当月のAPI利用数が上限の1000回に達しており、biz.candyhouse.coのサイト上からの
    リクエストも通らなくなっていることをユーザーが確認した。apikeyの失効ではなく上限到達による
    拒否である可能性が高いため、**再発行では解消しない前提で扱う**。翌月のカウンタリセット待ちか、
    プランの引き上げか、どちらを採るかを決める。アプリ側が上限を使い切る原因はBL-142で対応済み。
  優先度: P1
  状態: 未着手
  担当: ユーザー
  完了条件: >-
    上限到達が解消（翌月のリセットまたはプラン引き上げ）された状態で、ウィジェットの状態取得が
    「施錠中」または「解錠中」を表示し、Tileからの施錠/解錠が成功すること
  根拠: >-
    実資格情報とアカウント設定の操作を伴うため自律ループ実行モードでは実行できない
    （rules/guardrails-unified.v1.md 12.5）。アプリの実装に起因しない事象だが、
    アプリが動作しない状態の解消が先行するため優先度をP1とした。
    上限到達時の応答がHTTP 429ではなく403である点は、CANDY HOUSE側の実装によるものと推測しており
    未確認（アプリ側は401 / 403 / 429をいずれも「利用者が対処できる失敗」として同じ扱いにしており、
    表示は「認証エラー」で統一している。BL-140で対応済み）。
  依存: []

- id: BL-138
  区分: 人手検証
  タスク内容: >-
    0.11.0 のリリースタグ v0.11.0 と GitHub Releases を作成する。v0.9.0（BL-098）・
    v0.10.0（BL-116）と同じ扱いで、クローズドテストの両トラックが「公開中」になってから作成する
    （2026-09-18 に公開中を確認済み）。本文は docs/RELEASE_NOTES.md の 0.11.0 の内容を転記する。
    手順は Skill github-release-publish に従い、タグの push とリリース作業ブランチのマージのみ
    ユーザーが実行する。
  優先度: P2
  状態: 未着手
  担当: ユーザー
  完了条件: >-
    v0.11.0 の注釈付きタグが main に作成・push され、GitHub Releases に 0.11.0 の
    リリースノートを本文とするリリースが公開されていること
  根拠: >-
    タグの push と GitHub への発信を伴うため自律ループ実行モードでは実行できない
    （rules/guardrails-unified.v1.md セクション12.2）。BL-127 のストア掲載情報の転記とは
    独立して進められるため別項目として起票した。
  依存:
    - BL-127

- id: BL-127
  区分: 人手検証
  タスク内容: >-
    ウィジェットを含む版（0.11.0 想定）をクローズドテストへ配信する。mobile の versionCode を上げて
    電話・タブレット系トラックへアップロードする。wear は BL-119 の移設で成果物が変わるため、
    ウォッチ側のリリースも出すか（出す場合は WEAR_VERSION_CODE を上げて Wear OS 専用トラックへ）を
    判断する。2026-09-17 のデバッグ版での実機検証で Tile・Complication にリグレッションが無いことを
    確認済みで、BL-130 で wear の依存も変わるため、ウォッチ側も出すのが既定。配信後、Play Console の androidx.fragment:fragment の警告が消えたことを確認する。あわせて BL-124 で更新したストア掲載情報（docs/store/STORE_LISTING.md の短い説明・詳細な説明・対象デバイスと、
    docs/RELEASE_NOTES.md の 0.11.0 のストア掲載用の要約）を Play Console へ
    転記し、docs/CLOSED_TEST.md の参加条件変更を募集先（Qiita記事・X）へ反映する。配信後は
    RELEASE_NOTES.md の「未リリース」を配信日へ更新し、USER_GUIDE.md 等の「0.11.0以降」の注記を
    残すか整理するかを判断する。
    2026-09-17 にウォッチ側も出す既定のまま、versionName 0.11.0 / mobile の versionCode 6 /
    wear の versionCode 1004 で署名付き AAB 2件をビルド済み（scripts/version.properties を
    同値へ更新）。2026-09-18 に両トラック（電話・タブレット / Wear OS 専用）で公開中になった。
    配信日を RELEASE_NOTES.md・CLOSED_TEST.md へ反映済み。残りは、ストア掲載情報（短い説明・
    詳細な説明）の Play Console への転記と反映後の STORE_LISTING.md の注記削除、
    androidx.fragment:fragment の警告消失の確認、テスター数の維持確認、募集先（Qiita記事・X）
    への反映。ストア掲載情報はリリースと同じ送信へ混ぜず、リリース公開後に別送信で出す
    （差し戻し時にアプリ更新の配信まで止まるため）。2026-09-18 に掲載アセットも更新済みで、
    機能グラフィックの文言をウィジェット対応へ差し替え、スマートフォン用スクリーンショットを
    エミュレータで6枚撮り直した（ウィジェットの施錠中・解錠確認・解錠中・対象選択、資格情報設定、
    ヘルプメニュー）。これらも同じ送信で Play Console へ差し替える。アプリアイコンと Wear OS 用
    スクリーンショット6枚は現行のままでよい（アイコンの図柄は未変更、wear 側の変更は表示に
    影響しないことを差分で確認済み）。Play Console 側の作業手順とチェックリストは
    docs/store/PLAY_CONSOLE_STEPS.local.md の「次回の作業（BL-127）」へ記載した。
  優先度: P2
  状態: 進行中
  担当: ユーザー
  完了条件: >-
    ウィジェットを含む版がクローズドテストの電話・タブレット系トラックで公開され、テスター数が
    減っていないこと、および androidx.fragment:fragment の警告が消えたことを Play Console で確認していること
  根拠: >-
    Play Console の操作と外部への発信を伴うため自律ループ実行モードでは実行できない
    （rules/guardrails-unified.v1.md セクション12.2）。クローズドテストの14日間のカウント（BL-106）は
    テスターのオプトイン状態で数えるため、版の更新自体では途切れない。
  依存: []

- id: BL-128
  区分: UX改善
  タスク内容: >-
    ウィジェットの第2段階として、サイズ別のレイアウトを用意する。第1段階（BL-121）は Tile 相当の
    1サイズのみのため、ホーム画面の1マス（1x1）に置けるコンパクト表示（状態アイコンと短い状態文言、
    タップで BL-122 と同じ操作）と、横長（例 4x1）で複数台を並べる表示を検討する。wear の Complication
    （SHORT_TEXT / LONG_TEXT）と情報量を揃え、ウィジェットだけにある表示要素を増やしすぎない。
  優先度: P3
  状態: 未着手
  担当: AIエージェント
  完了条件: >-
    サイズごとに表示する要素と、wear の Tile / Complication との対応が DESIGN.md に定義され、
    各サイズでタップ操作と文言の省略が起きないことをユニットテストと実機スクリーンショットで
    示せること
  根拠: >-
    第1段階をシンプルに保つ方針により後続段階へ分けた。複数台を1つのウィジェットに並べる表示は
    wear 側に無い機能になるため、採否は実装前にユーザーへ確認する。
  依存: []

- id: BL-129
  区分: UX改善
  タスク内容: >-
    ウィジェット操作の結果フィードバックを改善する。wear はハプティクスで成否を区別しているが、
    第1段階（BL-122、実装済み）のウィジェットは失敗時に操作前の表示へ戻すだけで、失敗したことが分かりにくい。
    失敗を一定時間表示する状態（例「失敗」表示とグレー背景）の追加、スマホのハプティクス
    （wear の SesameHapticPatternResolver と同じ成否パターン）の再生、状態が古い場合の表示の区別
    （wear の STATUS_STALE_THRESHOLD_MILLIS = 30秒相当の鮮度判定と自動状態取得）を検討する。
  優先度: P3
  状態: 未着手
  担当: AIエージェント
  完了条件: >-
    失敗が表示または振動で成功と区別できること、古い状態で操作する前に状態取得が行われることが
    ユニットテストで示されていること。wear 側の挙動と差がある場合はその理由が DESIGN.md に
    書かれていること
  依存: []

- id: BL-106
  区分: 人手検証
  タスク内容: Google Playの製品版公開に必要なクローズドテスト（12人以上が14日間連続でオプトイン）を
    実施し、製品版へのアクセスを申請する。テスター管理はメールアドレスのリストではなく
    Googleグループ方式を採用し、参加者が自分でグループへ参加してオプトインする自己完結フローに
    する（ユーザーがメールアドレスを収集・保管しない方針）。手順の詳細と現在地は
    docs/store/PLAY_CONSOLE_STEPS.local.md の「次回の再開ポイント」フェーズ8（クローズドテスト）を
    参照する。2026-09-14に0.10.0が両トラックで公開中となり、2026-09-15にQiita記事とXで募集を開始した
  優先度: P2
  状態: 進行中
  担当: ユーザー
  完了条件: クローズドテストで12人以上が14日間連続でオプトインした状態を満たし、製品版への
    アクセス申請が承認される
  根拠: Claudeによる代替実施は不可。Play ConsoleとGoogleグループはブラウザ専用の操作であり、
    テスターの募集も外部への発信にあたるため自律ループ実行モードでは実行できない
    （rules/guardrails-unified.v1.md セクション12.2）
  依存: []


```
<!-- COPILOT_RECORDS:END -->
